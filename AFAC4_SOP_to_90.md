# AFAC4 提升 SOP：从 V31 稳定冲分到高分段

## 0. 先把目标说清楚

这份 SOP 不是“照做必到 90 分”的承诺，而是基于当前代码、实验记录和评分公式，重新整理的一套**现实可执行的冲分路线**。

当前最优基线：

- V31：约 60% Accuracy，约 3.19M Token，TokenScore 约 0.362，最终分约 48.5
- 当前主要矛盾：multi 题准确率依赖大段证据，但大段证据吃 token；压缩证据又容易漏关键信息
- 所以真正路线不是“继续加 prompt”，而是：**保准确率、降 token、再用验证机制补 multi 的漏选/过选**

核心判断：

1. 不改架构，最多小涨，很难超过 55-60
2. 小改证据链路，目标 55-65 比较现实
3. 重写为选项级事实验证架构，才有机会冲 70-80+
4. 90 分需要接近 96%-98% Accuracy 且总 token 控制在 1.0M-1.4M，属于极限目标，需要事实库、逐题精修和多轮提交，不是单次 SOP 自动达成

---

## 1. 评分公式重新校准

公式：

```text
FinalScore = 100 × Accuracy × (0.7 + 0.3 × TokenScore)
TokenScore = max(0, min(1, (5,000,000 - TotalTokens) / 5,000,000))
```

关键修正：之前版本里“95% Acc + 0.50 TS = 90 分”的判断是错的。

实际：

```text
95 × (0.7 + 0.3 × 0.50) = 80.75
```

也就是说，**90 分不是 95% 准确率 + 2.5M token 能达到的**。

几个真实门槛：

```text
如果 Acc = 100%，需要 TS ≥ 0.667，总 token ≤ 1.665M
如果 Acc = 98%， 需要 TS ≥ 0.728，总 token ≤ 1.36M
如果 Acc = 97%， 需要 TS ≥ 0.759，总 token ≤ 1.20M
如果 Acc = 96%， 需要 TS ≥ 0.792，总 token ≤ 1.04M
如果 Acc = 95%， 需要 TS ≥ 0.825，总 token ≤ 0.875M
```

结论：

- 60 分段：可以靠 V31 上继续优化
- 70 分段：需要明显降低 token，同时提升 multi 准确率
- 80 分段：需要选项级证据与验证机制
- 90 分段：需要事实库 + 极低 token + 近乎满分准确率

---

## 2. 当前项目的真实问题

### 2.1 V31 为什么是当前最优基线

V31 本质上是一个折中版本：

```text
tf/mcq：走 V30 精炼证据，省 token
multi：走 V22 大段证据，保准确率
```

这个方向是对的。实验已经证明：

- V22：准确率高一些，但 token 太高
- V30：token 很省，但 multi 准确率崩
- V31：两者折中，所以目前最高

所以后续不能推倒 V31，应该把 V31 当基线做局部替换。

### 2.2 最大瓶颈不是模型，而是证据组织方式

现在 multi 题依赖：

```text
截取 45K-90K 原文 → 让 LLM 自己找关键句 → 一次性输出多选答案
```

这个方式有三个问题：

1. token 贵，导致 TokenScore 很低
2. 证据多但不结构化，模型容易漏读、串文档、误算数字
3. 多选完全匹配评分下，错一个字母就是 0 分

所以后续提升的核心不是“喂更多”，而是“喂得更准”。

### 2.3 V30 的教训必须记住

V30 说明：

```text
过度结构化 / 过度压缩 = token 省了，但准确率大幅下降
```

所以任何新证据提取方案，必须先做小样本对照：

```text
旧证据是否包含关键原文？
新证据是否也包含？
新证据是否让答案变化？
变化是修正还是误伤？
```

没有这个对照，不能全量替换。

---

## 3. 总体路线

推荐把目标拆成三个版本，而不是一口气冲 90。

### V32：现实冲分版，目标 52-56

目标：在不明显降低 Acc 的前提下，把 V31 的 token 降下来。

核心动作：

```text
只改 multi 证据提取，不改答案后处理，不改 tf/mcq，不引入 LLM verifier
```

预期：

```text
Acc：尽量保持 58%-60%
Token：3.19M → 2.3M-2.7M
Score：48.5 → 52-56
```

### V33：高风险验证版，目标 56-62

目标：对容易过选/漏选的 multi 题做轻量 verifier。

核心动作：

```text
只对高风险 multi 题触发选项级验证
每题最多验证 2-3 个选项
验证 prompt 必须小于 2K-3K
```

预期：

```text
Acc：60% → 65%-70%
Token：控制在 2.6M-3.1M
Score：56-62
```

### V40：架构重写版，目标 65-75+

目标：把推理从“题目级”改成“选项级事实判定”。

核心动作：

```text
question → option claims → fact lookup → minimal evidence pack → option verdicts → final answer
```

预期：

```text
Acc：70%-80%+
Token：2.0M-2.8M
Score：65-75+
```

### V50：极限冲榜版，目标 80-90

目标：事实库 + 逐题精修 + 极低 token。

核心动作：

```text
离线事实库覆盖大部分题
在线只做最小证据判断
最后剩余错题逐题定制策略
```

现实判断：这是一个新项目级别的工作，不是小改。

---

## 4. V32 执行 SOP：先保 Acc 再降 token

V32 是下一步最应该做的版本。

### 4.1 改动原则

只允许改一个变量：**multi 证据构造方式**。

禁止同时改：

- prompt
- postprocessor
- fallback A
- tf/mcq 链路
- 模型参数
- 答案解析逻辑

否则退分后无法归因。

### 4.2 证据压缩目标

不要从 90K 一步压到 20K，风险太大。

建议分三档实验：

```text
V32a：multi 证据上限 60K
V32b：multi 证据上限 45K
V32c：multi 证据上限 30K
```

先跑小样本，不要直接全量。

### 4.3 新证据包结构

每道 multi 题的证据包建议由五部分组成：

```text
1. 文档头部摘要 / 开头 3K-5K
2. 题干关键词命中的上下文
3. 每个选项关键词命中的上下文
4. 条款号 / 年份 / 金额 / 百分比的精准定位上下文
5. V31 原本靠前的高相关 chunk 作为兜底
```

不要只依赖关键词搜索，因为金融文档常见同义表达。

### 4.4 关键词提取规则

从题干和选项里提取这些信息：

```text
实体：公司名、产品名、基金名、法规名、合同名
时间：年份、日期、报告期、季度
数字：金额、比例、天数、期限、倍数
科目：营收、净利润、现金流、保费、赔付、资产、负债
动作：增长、下降、派发、回购、披露、禁止、应当、可以
条款：第X条、第X章、第X款
否定：不、未、不得、无、没有、除外
```

### 4.5 证据去重规则

按位置去重，不要只按文本前 100 字符去重。

建议：

```text
同一 doc 内，如果两个命中位置距离 < 800 字，则合并
合并后的窗口控制在 1500-2500 字
每个选项最多保留 2-3 个窗口
每个 doc 至少保留 1 个窗口，避免跨文档题只看到一个 doc
```

### 4.6 小样本验证集

先选 20 道题：

```text
10 道 V31 大概率正确的 multi 题：检查新证据是否误伤
10 道 V31 错误/不稳定的 multi 题：检查新证据是否可能修正
```

验证标准：

```text
如果 V32 比 V31 少对 ≥3 题：不能全量
如果 V32 与 V31 持平且 token 降 ≥25%：可以全量
如果 V32 多对 ≥2 题且 token 降 ≥15%：优先全量
```

### 4.7 V32 全量提交门槛

满足以下条件再跑全量：

```text
20 题小样本准确率不低于 V31
multi 平均输入 token 降低至少 25%
无空答案
答案分布没有明显塌缩到 A 或 ABC
```

---

## 5. V33 执行 SOP：高风险 multi 轻量验证

V33 在 V32 成功后再做。不要 V32 还没稳定就加 verifier。

### 5.1 什么题触发验证

只验证高风险题，控制 token。

高风险条件：

```text
1. Stage1 输出 ABC 或 ABCD
2. Stage1 输出只有 A，但题型是 multi
3. raw_response 里出现“不确定 / 可能 / 原文未明确 / 推测”
4. 涉及两个以上 doc 的对比题
5. 选项里包含数字计算、同比、比例、阈值
6. 选项里有“均、都、全部、任何、不得、应当、必须”
```

每轮最多验证 25-35 道题，超过则按风险分排序截断。

### 5.2 验证对象选择

不要四个选项全验，太贵。

推荐：

```text
如果 Stage1 选了 3-4 个：优先验证被选中的选项，防过选
如果 Stage1 只选 1 个：验证 1-2 个未选但高相关选项，防漏选
如果是跨文档题：只验证涉及“均/都/分别/相比”的选项
```

### 5.3 Verifier 输出必须结构化

Verifier 不要让模型长篇解释。只要：

```text
VERDICT: YES / NO / UNKNOWN
REASON: 一句话
EVIDENCE: 原文短句
```

判定规则：

```text
YES：原文明确支持选项完整表述
NO：原文明确矛盾，或关键数字/主体/时间不一致
UNKNOWN：证据不足，不能确认
```

### 5.4 最终合并规则

保守合并，避免 verifier 误删正确答案。

```text
如果 Stage1 选中，Verifier=YES：保留
如果 Stage1 选中，Verifier=NO：删除
如果 Stage1 选中，Verifier=UNKNOWN：默认保留，除非该选项明显是数字/条款题
如果 Stage1 未选，Verifier=YES：新增
如果 Stage1 未选，Verifier=UNKNOWN/NO：不新增
```

### 5.5 V33 成功标准

小样本验证：

```text
至少修正 3 道 multi 错题
误伤不超过 1 道原本正确题
新增 token 控制在 300K-500K
```

达不到这个标准，不跑全量。

---

## 6. V34 执行 SOP：规则 verifier，优先替代 LLM verifier

如果 V33 token 太贵，就做 V34：规则验证器。

### 6.1 财报类规则

重点识别：

```text
营业收入 / 营收
归母净利润 / 净利润
扣非净利润
经营活动现金流
同比增长 / 同比下降
毛利率 / 净利率
资产负债率
每股收益
分红 / 派息 / 回购
```

规则：

```text
选项含年份 → 证据必须同年份
选项含增长/下降 → 必须找到两期数字或明确同比描述
选项含“超过/低于/不少于” → 必须做数值比较
选项含“最高/最低” → 必须有多个对象比较
```

### 6.2 法规类规则

重点识别：

```text
应当 / 可以 / 不得 / 禁止 / 鼓励
期限：30日、30个工作日、5日内
主体：金融机构、客户、监管机构、工作人员
义务：报告、披露、保存、识别、审核
```

规则：

```text
“应当”不能等价于“可以”
“不得”不能等价于“不宜”
“工作日”和“自然日”必须区分
主体错配直接判 NO
条款号命中优先级最高
```

### 6.3 保险合同类规则

重点识别：

```text
身故保险金
现金价值
保险金额
基本保险金额
等待期
犹豫期
免责条款
给付比例
```

规则：

```text
身故保险金 ≠ 现金价值
保险金额 ≠ 保费
等待期内/外必须区分
免责条款中出现的情形一般不赔
涉及计算必须列公式
```

### 6.4 规则 verifier 的作用

规则 verifier 不一定直接给答案，而是给 LLM 一个强约束证据卡：

```text
选项 B：数字 2024 年营收命中，2025 年营收命中，计算同比为 3.2%，选项声称 >5%，规则判定 NO
选项 C：条款号命中，但主体从“金融机构”变成“客户”，规则判定 NO
```

这样可以减少 LLM 脑补。

---

## 7. V40 执行 SOP：选项级事实验证架构

当 V32/V33 已经验证有效后，再进入 V40。

### 7.1 新架构

```text
原问题
  ↓
题型识别 + 领域识别
  ↓
选项拆解为 claim
  ↓
每个 claim 定向找 facts/evidence
  ↓
规则 verifier 先判一轮
  ↓
LLM verifier 只处理 UNKNOWN
  ↓
合并 option verdicts
  ↓
输出最终答案
```

### 7.2 需要新增的模块

```text
agent/claim_parser.py
agent/evidence_builder_v40.py
agent/option_verifier.py
agent/fact_store.py
agent/domain_verifiers/financial.py
agent/domain_verifiers/regulation.py
agent/domain_verifiers/insurance.py
agent/pipeline_v40.py
```

### 7.3 Claim 拆解格式

每个选项拆成：

```json
{
  "option": "B",
  "claim_text": "2025年营业收入同比增长超过5%",
  "entities": ["营业收入"],
  "time": ["2025", "2024"],
  "numbers": ["5%"],
  "operator": ">",
  "domain": "financial_report",
  "verification_type": "numeric_compare"
}
```

### 7.4 最小证据包

每个选项只喂：

```text
claim
规则抽取结果
2-4 段最相关原文
必要的上下文说明
```

不要再喂整题 90K 原文。

目标 token：

```text
tf：4K-6K / 题
mcq：8K-12K / 题
multi：12K-20K / 题
```

### 7.5 V40 成功标准

不是直接看全量分数，先看小样本：

```text
20 道 multi：比 V31 多对 ≥5 道，且 token 降 ≥30%
10 道财报题：数字类错误明显减少
10 道法规题：应当/可以/不得类错误明显减少
```

达不到这个标准，说明 V40 还没成熟。

---

## 8. 实验纪律

这是最重要的部分。

### 8.1 每次只改一个变量

错误示例：

```text
同时改证据提取 + prompt + verifier + 后处理
```

这样退分后完全不知道是谁的问题。

正确示例：

```text
V32a：只改 multi 证据上限 60K
V32b：只把 60K 改成 45K
V33a：只加高风险 verifier，不改证据
```

### 8.2 必须小样本过关再全量

每次全量前必须跑：

```text
20 题小样本
至少包含 10 道 V31 正确题 + 10 道 V31 错误题
记录每题：旧答案、新答案、token、是否修正、是否误伤、原因
```

### 8.3 不要修掉 fallback A

当前答案解析里的 fallback A 虽然不优雅，但它是安全网。

规则：

```text
不删除 fallback A
不大改 postprocessor
任何答案解析修改必须单独成版本验证
```

### 8.4 每次提交前检查

必须检查：

```text
无空答案
答案只包含 A/B/C/D
单选题答案长度为 1
multi 题不能大量坍缩到 ABC 或 ABCD
A 的比例不能异常低，也不能异常高
token 预算不超过预期
```

### 8.5 Acc 优先于 TokenScore

公式里 Accuracy 权重更大。粗略记：

```text
多答对 1 题，通常值得多花 80K-120K token
如果一个改动省 500K token 但少对 3 题，大概率不划算
如果一个改动多对 3 题但多花 300K token，大概率划算
```

---

## 9. 推荐执行顺序

### Day 1：V32a

任务：

```text
实现 multi evidence_builder_v32
证据上限先设 60K
保留 V31 其他链路
跑 20 题小样本
```

通过标准：

```text
小样本准确率不低于 V31
token 降 ≥15%
```

### Day 2：V32b

任务：

```text
证据上限从 60K 降到 45K
继续跑同一组 20 题
对比误伤题
```

通过标准：

```text
准确率不低于 V32a - 1 题
token 降 ≥25%
```

### Day 3：V32 全量

任务：

```text
选择 V32a/V32b 中小样本表现最好的版本
跑全量
```

目标：

```text
Score 52+
如果能 55+，说明证据压缩方向成立
```

### Day 4-5：V33a

任务：

```text
实现高风险题识别
只对 20-30 道 multi 触发 verifier
每题最多验证 2 个选项
```

通过标准：

```text
小样本多修正 ≥3 题
误伤 ≤1 题
新增 token ≤300K
```

### Day 6：V33 全量

目标：

```text
Score 56-62
```

### Day 7+：V34/V40

如果 V33 有效，继续做规则 verifier 和选项级架构。

如果 V33 无效，回滚 verifier，只保留 V32 的证据压缩收益。

---

## 10. 90 分的真实路线

如果最终目标仍然是 90，必须接受下面这些事实：

```text
1. 不能靠 2M-3M token 到 90
2. 不能靠整题大段原文到 90
3. 不能靠一次 CoT 到 90
4. 不能靠全量 LLM verifier 到 90
5. 必须靠离线事实库 + 最小证据包 + 逐题精修
```

90 分版本需要：

```text
文档事实库覆盖 80% 以上题目
大部分题每题 token 控制在 5K-12K
只有极难题使用 30K-50K
准确率接近 97%
最后 5-8 道错题人工逐题打补丁
```

这是 V50，不是 V32/V33。

所以短期不要把“90 分”当作每日验收目标。正确验收顺序是：

```text
48.5 → 52 → 56 → 60 → 65 → 70 → 75 → 80+
```

每跨一个台阶，都需要真实提交分数验证。

---

## 11. 最终建议

现在立刻执行：

```text
先做 V32，不做 V40。
```

因为 V32 风险最低、收益最确定：

- 不动答案解析
- 不动 tf/mcq
- 不动模型策略
- 只压 multi 证据
- 能直接验证“是否可以保 Acc 降 token”

如果 V32 失败，说明当前题集对大段原文依赖很强，后面所有压缩方案都要更谨慎。

如果 V32 成功，再做 V33 verifier。

如果 V33 也成功，再考虑 V40 选项级架构。

一句话：

> 当前最优策略不是直接重写架构冲 90，而是先用 V32/V33 把 V31 的 token 和 multi 错误打下来；只有证明“精准证据 + 轻量验证”有效，才值得进入 V40/V50。